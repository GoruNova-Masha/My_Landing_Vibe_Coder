Ты — Senior Frontend Developer, специализируешься на Next.js (App Router), TypeScript и Tailwind CSS. Твоя задача — создать и настроить секцию контактов «ContactSection» для лендинга «Vibe Coder» в техно‑стиле.

### Контекст и требования к почте (критически важно)
1.  **Публичный адрес (для красоты):** В верстке секции (в блоке контактов слева) должен отображаться стильный адрес `hello@vibecoder.studio`. Сделай его ссылкой `mailto:`, чтобы при клике открывался почтовый клиент пользователя. Это только для ручного письма.
2.  **Технический адрес (для приёма заявок):** Форма должна отправлять данные ТОЛЬКО на реальную рабочую почту `m.nemnyaseva@gmail.com`. 
    ⚠️ Ни в коем случае не используй `hello@vibecoder.studio` в логике отправки формы (fetch, action) или в настройках Formspree. Заявки должны приходить на `m.nemnyaseva@gmail.com`.

### Технические требования
1.  **Интеграция Formspree:**
    - Используй endpoint: `https://formspree.io/f/meajnojq`.
    - Реализуй отправку через `fetch` внутри функции `handleSubmit`.
    - Поля формы должны иметь атрибуты `name`: `name="name"`, `name="email"`, `name="message"`.
    - Добавь обработку состояний: `idle`, `sending`, `success`, `error`.
    - При успехе покажи сообщение: «Спасибо! Я получил ваше сообщение и отвечу в течение 24 часов».
    - При ошибке: «Произошла ошибка при отправке. Попробуйте позже».
    - Блокируй кнопку и поля ввода во время отправки (`disabled`).

2.  **LED‑рамка (Hover Effect):**
    - Оберни контейнер с формой в `div` с классами: `relative overflow-hidden group form-with-led`.
    - В `globals.css` добавь стили для псевдоэлемента `.form-with-led::before`, анимацию `@keyframes ledGlow` и логику `:hover`.
    - Эффект: при наведении должна появляться полупрозрачная рамка с циановым градиентом и мягким свечением (`box-shadow`). Анимация мерцания должна быть плавной, не раздражающей.

3.  **Анимация появления:**
    - Добавь класс `animate-form-enter` на саму форму или её обертку.
    - В `globals.css` должен быть `@keyframes formEnter` и класс `.animate-form-enter` с `forwards`, чтобы форма оставалась видимой после анимации.
    - Анимация: плавное появление снизу (`translateY`) с легким увеличением (`scale`).

4.  **Верстка и UX:**
    - Используй сетку: `grid grid-cols-1 md:grid-cols-2 gap-12`.
    - Слева: блок с контактами (заголовок, красивый email `hello@vibecoder.studio`, соцсети).
    - Справа: форма.
    - Сохрани монохромную палитру с циановыми акцентами (`cyan-300`, `cyan-400`).
    - Не меняй тексты заголовков и кнопки: «Готовы к проекту?», «Я — Vibe Coder...», кнопка «Отправить →».

### Структура файлов
1.  **Файл компонента:** `app/components/ContactSection.tsx` (или просто `ContactSection.tsx`, если структура другая). Используй `'use client'`, так как нужна обработка событий формы.
2.  **Файл стилей:** `globals.css`. Добавь туда все необходимые `@keyframes` и стили для `.form-with-led`.

### Критерии приёмки
- Форма отправляется на указанный endpoint.
- При наведении на форму появляется LED‑рамка.
- Форма плавно выезжает при загрузке.
- На сайте виден красивый адрес `hello@vibecoder.studio`, но все заявки приходят на `m.nemnyaseva@gmail.com`.
- В консоли нет ошибок TypeScript, CORS или 404.

### Что выдать в ответе
1.  Полный код компонента `ContactSection.tsx` (с логикой отправки, версткой контактов и классами для эффектов).
2.  Полный код необходимых стилей для `globals.css` (анимации и LED‑рамка).
3.  Краткое пояснение, куда вставить эти файлы и что проверить в настройках Formspree (напоминание оставить в получателях только рабочую почту).

Действуй максимально аккуратно: не ломай существующую верстку, не удаляй навигацию и футер. Твоя цель — точечно внедрить эту секцию, сохранив общий стиль проекта.
